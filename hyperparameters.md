| Name | Type | Current Value | Description | Notes |
|---|---|---|---|---|
| `background_function_strategy` | `int` | `1` | Background function computation strategy – this is the strategy used in part4a notebook for calculating the background function.<br><br>Possible options are: 1, 2, 3.<br><br>**1** – Calculate a background function per channel using all the fields of view in the dataset which are not flagged.<br><br>**2** – Calculate a background function per each channel and well using the fields of view in the train set which are not flagged and belong to the well.<br><br>**3** – Calculate a background function per each channel and grid position using the fields of view in the train set which are not flagged and belong to the same position (aka grid position) across different wells. | I would drop the concept of working on wells and grid positions. This stemmed from the idea of using different approaches to calculate background function (ref to notebook_4a) and then correcting it. I would drop this concept for the new pipeline which is under implementation. Or better, I would ignore it. Instead I would work under the assumption that background functions have already been calculated (using whatever strategy a user prefers) and I would use a function to fetch per each field of view the correct background function. Note that this is different from what we discussed. When we discussed I talked about using a file, such as a table, matching fields of view to process with their background functions. I changed my mind to using a function as I imagine that the use of a table could be difficult to parallelize. Please let me know if this is incorrect. The function to use for fetching the correct background per each field of view can be as simple as returning the field of view name. It would need to be adapted to the user's needs. Happy to also get your opinion. |
| `background_directory` | `str` | `r"Z:\AlessandroUlivi\_Data\projects\ACID\data\proc\background"` | Indicate the path to the directory storing the background function images. | |
| `metadata_directory` | `str` | `r"Z:\AlessandroUlivi\_Data\projects\ACID\data\proc\proc\_metadata"` | Indicate the path to the directory storing the metadata file.<br><br>**NOTE:** This is expected to be the `metadata_df` saved from either the part4a or part3 notebook. | |
| `fov_directory` | `str` | `r"Z:\AlessandroUlivi\_Data\projects\ACID\data\proc\fov"` | Indicate the path to the directory storing the fields of view. | |
| `output_directory` | `str` | `r"Z:\AlessandroUlivi\_Data\projects\ACID\data\proc\fov\_proc"` | Indicate the path to the directory where outputs will be saved.<br><br>**NOTE:** If the directory does not exist, the pipeline will try to create it. | |
| `metadata_file_name` | `str` | `"default"` | Name of the metadata file – this is expected to be the `metadata_df` saved from part4a notebook.<br><br>The following options are possible:<br>**1** – Write the full name (extension included) of the CSV file.<br>**2** – Write `"default"` or any other version with an uppercase letter (e.g. `"Default"`).<br>**3** – Leave an empty string: `""`.<br>**4** – Write `None` (note that this is not a string; there are no quotation marks, i.e. it is not `"None"`).<br><br>Options 2, 3, and 4 lead to the same behavior: the most recently saved CSV file will be used.<br><br>By default, the timestamp of the last file modification is used. Alternatively, the date saved in the file name can be used by setting `metadata_from_file_name` to `True` (see `utils.get_defaults.default_file_name` documentation). | |
| `background_function_timestamp` | `str` | `"default"` | The timestamp (aka the date) present in the file name of the background function image. This is used for selecting the background function image to use for the illumination correction in the part4b notebook.<br><br>The following options are possible:<br>**0** – **ONLY POSSIBLE FOR STRATEGY 1:** Write the full name (extension included) of the background function image file. In this case, the file will be selected based on the name and not on the timestamp.<br><br>**1** – Write the timestamp (aka the date) as a string (e.g. `"20251126"`). **NOTE:** The string must match the exact format used in the file name. For example, if the file name is `"251126_plate_layout.csv"`, the timestamp string should be `"251126"` and not `"20251126"`.<br><br>**ALSO NOTE:** If strategy 1 is used, only a single file with the timestamp in the name is expected in the background function directory. If strategy 2 or 3 is selected, a number of files corresponding to the number of wells or grid positions, respectively, are expected.<br><br>**2** – Write `"default"` or any other version with an uppercase letter (e.g. `"Default"`).<br><br>**3** – Leave an empty string: `""`.<br><br>**4** – Write `None` (note that this is not a string; there are no quotation marks, i.e. it is not `"None"`).<br><br>Options 2, 3, and 4 lead to the same behavior: the most recently saved OME-TIFF files will be used.<br><br>By default, the timestamp of the last file modification is used. Alternatively, the date saved in the file name can be used by setting `metadata_from_file_name` to `True` (see `utils.get_defaults.default_file_name` documentation).<br><br>**TECHNICAL NOTE:** The use of the timestamp is not strictly necessary. In particular, any string that allows the file(s) to be uniquely identified can be used to select the background function file(s). | I think that we can completely skip this different strategies scheme in the new pipeline which is under construction. As reported also in the notes for `background_function_strategy`, I would work on the assumption that the pipeline expects a function which allows to fetch the correct background function per each file. |
| `save_graphs` | `bool` | `True` | Indicate whether to save the graphs generated in this notebook. | |
| `graph_saving_directory` | `str` | `r"Z:\AlessandroUlivi\_Data\projects\ACID\develop\260319\_illumination\_correction"` | Graph saving directory – this is the directory where the graphs generated in this notebook will be saved. If `save_graphs` is True | |
| `is_train_column` | `str` | `"is_train"` | Parameters to select the train set – indicate the name of the column indicating whether the row belongs to the train or test set. | We can drop this parameter. I would work under the assumption that no train dataset has to be selected. Data preparation has to be done upfront to the new pipeline which is under construction. This includes separating train-validation-test datasets. The pipeline is agnostic to whether the files are the train, validation, test datasets or something else, it just gets input files, processes and measures them, irrespective of what they are. |
| `train_val` | `int` | `1` | Parameters to select the train set – indicate the value signalling that a row (aka a field of view) belongs to the train set. | As I wrote for `is_train_column` we can drop this parameter. I would work under the assumption that no train dataset has to be selected. Data preparation has to be done upfront to the new pipeline which is under construction. This includes separating train-validation-test datasets. The pipeline is agnostic to whether the files are the train, validation, test datasets or something else, it just gets input files, processes and measures them, irrespective of what they are. |
| `default_bg_funct_file_target` | `str` | `".ome.tif"` | Parameters used for importing the default background function – indicate the part of the file name used to select files in `background_directory` for the default file selection. | I would drop the concept of default background. As reported also in the notes for `background_function_strategy`, I would work on the assumption that the pipeline expects a function which allows to fetch the correct background function per each file. |
| `default_bg_funct_file_exclude` | `None` | `None` | Parameters used for importing the default background function – indicate the part of the file name used to exclude files in `background_directory` from the default file selection. Files containing this string in their name will be excluded from the default file selection. If `None`, no files will be excluded. | I would drop the concept of default background. As reported also in the notes for `background_function_strategy`, I would work on the assumption that the pipeline expects a function which allows to fetch the correct background function per each file. |
| `background_from_file_name` | `bool` | `False` | Parameters used for importing the default background function – indicate whether to extract the date information from the file name when selecting the default file. If `False`, the date will be extracted from the file's last modified timestamp; if `True`, it will be extracted from the file name.<br><br>If `background_from_file_name` is `True`, the parameters `background_default_separator` and `background_default_date_position` should be used to indicate, respectively, the separator used to split the file name into tokens and the position of the token containing the date information. In addition, the date format used in the file name should be indicated using `background_default_date_format`.<br><br>For example, if `background_from_file_name` is `True` and the file name is `"251126_plate_layout.csv"`, `"_"` is used as the separator, `0` as the date position, and `'%Y%m%d'` as the date format.<br><br>See `utils.get_defaults.default_file_name` for more details. | I would drop the concept of default background. As reported also in the notes for `background_function_strategy`, I would work on the assumption that the pipeline expects a function which allows to fetch the correct background function per each file. |
| `background_default_separator` | `str` | `'_'` | Parameters used for importing the default background function – indicate the separator used to split the file name and extract the information token containing the date. This parameter is only used if `background_from_file_name` is `True`. | I would drop the concept of default background. As reported also in the notes for `background_function_strategy`, I would work on the assumption that the pipeline expects a function which allows to fetch the correct background function per each file. |
| `background_default_date_position` | `int` | `0` | Parameters used for importing the default background function – indicate the position of the date information token in the list obtained after splitting the file name using `background_default_separator`. This parameter is only used if `background_from_file_name` is `True`. | I would drop the concept of default background. As reported also in the notes for `background_function_strategy`, I would work on the assumption that the pipeline expects a function which allows to fetch the correct background function per each file. |
| `background_default_date_format` | `str` | `'%Y%m%d'` | Parameters used for importing the default background function – indicate the format used for including the date in the file names used to select the default file. This information is important for ordering files by date. This parameter is only used if `background_from_file_name` is `True`. | I would drop the concept of default background. As reported also in the notes for `background_function_strategy`, I would work on the assumption that the pipeline expects a function which allows to fetch the correct background function per each file. |
| `background_default_reverse` | `bool` | `True` | Parameters used for importing the default background function – if `True`, the file with the most recent date will be returned by default; if `False`, the opposite will be returned. | |
| `background_default_assert_file_number` | `bool` | `True` | Parameters used for importing the default background function – indicate whether to assert that the number of files matching the default file selection criteria is exactly equal to `background_default_number_of_files_expected`.<br><br>If `True`, an assertion error will be raised if the number of files matching the criteria is not exactly equal to `background_default_number_of_files_expected`. If `False`, no assertion will be made and the function will return all files matching the default file selection criteria.<br><br>It is recommended to set this parameter to `True` in combination with `background_default_number_of_files_expected` set to `1` when `background_function_strategy` is `1`, since in this case only one background function file is expected to be used for the illumination correction. When `background_function_strategy` is `2` or `3`, the expected number should correspond to the number of wells or grid positions, respectively. | I would drop the concept of default background. As reported also in the notes for `background_function_strategy`, I would work on the assumption that the pipeline expects a function which allows to fetch the correct background function per each file. |
| `background_default_number_of_files_expected` | `int` | `1` | Parameters used for importing the default background function – indicate the number of files expected to be returned by the default selection criteria. This parameter is only considered when `background_default_assert_file_number` is set to `True`. It is recommended to set this parameter to `1` when `background_function_strategy` is `1`, since only one background function file is expected. When `background_function_strategy` is `2` or `3`, it should correspond to the number of wells or grid positions, respectively. | I would drop the concept of default background. As reported also in the notes for `background_function_strategy`, I would work on the assumption that the pipeline expects a function which allows to fetch the correct background function per each file. |
| `default_metadata_file_target` | `str` | `".csv"` | Parameters used for importing the default metadata dataframe – indicate the part of the file name used to select files in `metadata_directory` for the default file selection. Files containing this string in their name will be selected. If `None`, all files will be selected. | this is a general parameter which is common across notebooks |
| `default_metadata_file_exclude` | `None` | `None` | Parameters used for importing the default metadata dataframe – indicate the part of the file name used to exclude files in `metadata_directory` from the default file selection. Files containing this string in their name will be excluded. If `None`, no files will be excluded. | this is a general parameter which is common across notebooks |
| `metadata_from_file_name` | `bool` | `False` | Parameters used for importing the default metadata dataframe – indicate whether to extract the date information from the file name when selecting the default file. If `False`, the date will be extracted from the file's last modified timestamp; if `True`, it will be extracted from the file name.<br><br>If `metadata_from_file_name` is `True`, the parameters `metadata_default_separator` and `metadata_default_date_position` should be used to indicate, respectively, the separator used to split the file name into tokens and the position of the token containing the date information. In addition, the date format used in the file name should be indicated using `metadata_default_date_format`.<br><br>For example, if `metadata_from_file_name` is `True` and the file name is `"251126_plate_layout.csv"`, `"_"` is used as the separator, `0` as the date position, and `'%Y%m%d'` as the date format.<br><br>See `utils.get_defaults.default_file_name` for more details. | this is a general parameter which is common across notebooks. We can drop the concept of getting the default file from its name and only use its timestamp. |
| `metadata_default_separator` | `str` | `'_'` | Parameters used for importing the default metadata dataframe – indicate the separator used to split the file name and extract the information token containing the date. This parameter is only used if `metadata_from_file_name` is `True`. | this is a general parameter which is common across notebooks. We can drop the concept of getting the default file from its name and only use its timestamp. |
| `metadata_default_date_position` | `int` | `0` | Parameters used for importing the default metadata dataframe – indicate the position of the date information token after splitting the file name using `metadata_default_separator`. This parameter is only used if `metadata_from_file_name` is `True`. | this is a general parameter which is common across notebooks. We can drop the concept of getting the default file from its name and only use its timestamp. |
| `metadata_default_date_format` | `str` | `'%Y%m%d'` | Parameters used for importing the default metadata dataframe – indicate the format used for including the date in the file name to be opened by default. This parameter is only used if `metadata_from_file_name` is `True`. | this is a general parameter which is common across notebooks. We can drop the concept of getting the default file from its name and only use its timestamp. |
| `metadata_default_reverse` | `bool` | `True` | Parameters used for importing the default metadata dataframe – if `True`, the file with the most recent date will be returned by default; if `False`, the opposite will be returned. | this is a general parameter which is common across notebooks |
| `channel_axis` | `int` | `0` | Parameters for background illumination correction – channel axis to be passed to background calculating functions in order to calculate background functions per channel. This is the axis along which the channels are organized in the field of view arrays.<br><br>For example, if the field of view arrays have shape `(channels, height, width)`, the channel axis is `0`. | this is a general parameter. It was wrong of me to put it under parameters specific for background function, it can be generalized and used across notebooks. |
| `method` | `str` | `"division"` | Parameters for background illumination correction – the method used for illumination correction. This can be either `"subtraction"` or `"division"`. | I would completely eliminate this option. To be precise, I would try, if possible, to develop a workflow which works both when correcting the background by division and when correcting the background by subtraction. However even when this implementation is possible, I would not control it with a variable such as the present. Instead I would split the current functions which allow to do background correction both by division and substruction (image_processing/correct_background/correct_background_nd and image_processing/correct_background/correct_background) into independent functions per each method. I would then put it on the user to call the appropriate. The reason I would do this is to leave flexibility for the implementation of potential future different methods |
| `offset` | `int` | `400` | Parameters for background illumination correction – the offset to subtract from the image before correcting for the illumination. | I would change the term illumination correction into background correction in the description |
| `epsilon` | `float` | `1e-8` | Parameters for background illumination correction – the small constant to add to the background in order to avoid zero numbers in the background function, which would lead to mathematical instability (e.g. division by zero). | this parameter is only relevant for background correction by division. I would change the term illumination correction into background correction in the description |
| `output_dtype` | `np.dtype` | `np.float32` | Parameters for background illumination correction – the data type of the image corrected for illumination. | the image corrected for background is the output image. I would change the term illumination correction into background correction  in the description |
| `working_dtype` | `np.dtype` | `np.float32` | Parameters for background illumination correction – the data type to use for the mathematical operations (subtraction and division) during illumination correction.<br><br>The same data type is used for both the image to correct and the background function. | I would change the term illumination correction into background correction  in the description |
| `rescale_background` | `str` or `None` | `'max'` | Parameters for background illumination correction – whether or not to rescale the background function before carrying out illumination correction.<br><br>Three options are available:<br>**1** – `None`: no rescaling is done.<br>**2** – `"max"`: the background function is divided by the maximum value. The resulting normalized background function has 1 as its maximum value.<br>**3** – `"minmax"`: the background function is min-max normalized using `(background - min) / (max - min + epsilon)`. | I would think at a better way of calling this option. For example by passing a rescaling function as callable input to the background correction function. This would leave more flexibility for implementing different methods. Second note: rescaling is only important if background correct is via division. I would change the term illumination correction into background correction in the description |
| `clip_corrected_image` | `bool` | `False` | Parameters for background illumination correction – whether or not to clip the image corrected for illumination. If `True`, the image after illumination correction is clipped to the `min_clip_value`, `max_clip_value` range.<br><br>For example, when subtraction is used, the result can contain negative values. It is possible to set them to `0` by setting `clip_corrected_image=True` and `min_clip_value=0` (irrespective of `max_clip_value`). | I would change the term illumination correction into background correction in the description |
| `min_clip_value` | `int` | `0` | Parameters for background illumination correction – the minimum value to which the illumination-corrected image is clipped if `clip_corrected_image` is set to `True`. | I would change the term illumination correction into background correction in the description |
| `max_clip_value` | `None` | `None` | Parameters for background illumination correction – the maximum value to which the illumination-corrected image is clipped if `clip_corrected_image` is set to `True`. | I would change the term illumination correction into background correction in the description |
| `offset_background` | `bool` | `True` | Parameters for background illumination correction – whether or not to offset the background function before carrying out illumination correction.<br><br>Two options are available: `True` or `False`. | I would change the term illumination correction into background correction in the description |
| `zero_kwargs` | `None` | `None` | Parameters for background illumination correction – additional parameters to pass to the initial container of the illumination-corrected image during the correction process.<br><br>See the function `correct_background` within the `image_processing.correct_background.py` script for further details. | I would change the term illumination correction into background correction in the description |
| `verbose` | `bool` | `True` | Parameters for background illumination correction – select whether to print process information during background correction. | this parameter can be removed |
| `fov_column_name` | `str` | `"ome_tif_file_name"` | Parameters for background illumination correction – field of view column name in the metadata dataframe. | this is a general parameter. It was wrong of me to put it under parameters specific for background function, it can be generalized and used across notebooks. I would change the term illumination correction into background correction in the description |
| `well_column_name` | `str` | `"well"` | Parameters for background illumination correction – well column name in the metadata dataframe.<br><br>This parameter is used if strategy 2 is selected to calculate the background function. | First note: I would drop the concept of working on wells and grid positions. This stemmed from the idea of using different approaches to calculate background function (also ref to `background_function_strategy`) and then correcting it. As I wrote also there, I would drop this concept for the new pipeline which is under implementation. Instead I would work under the assumption that background functions have already been calculated and I would use a function to fetch per each field of view the correct background function. Second note: if this parameter is kept, this is a general parameter. It was wrong of me to put it under parameters specific for background function, it can be generalized and used across notebooks. Third note: if this parameter is kept, I would change the term illumination correction into background correction in the description. |
| `gridpos_column_name` | `str` | `"scene_name"` | Parameters for background illumination correction – within-well position column name in the metadata dataframe. This is used to identify the position of the field of view within the well.<br><br>49 fields of view were acquired per each well, in a 7×7 grid.<br><br>This parameter is used if strategy 3 is selected to calculate the background function. | First note: I would drop the concept of working on wells and grid positions. This stemmed from the idea of using different approaches to calculate background function (also ref to `background_function_strategy`) and then correcting it. As I wrote also there, I would drop this concept for the new pipeline which is under implementation. Instead I would work under the assumption that background functions have already been calculated and I would use a function to fetch per each field of view the correct background function. Second note: if this parameter is kept, this is a general parameter. It was wrong of me to put it under parameters specific for background function, it can be generalized and used across notebooks. Third note: if this parameter is kept, I would change the term illumination correction into background correction in the description. |
| `null_value` | `float` | `np.nan` | Parameters for background illumination correction – what is expected to be and what will be used as the null value. | this is a general parameter. It was wrong of me to put it under parameters specific for background function, it can be generalized and used across notebooks. I would change the term illumination correction into background correction in the description |
| `proc_img_meta_date_name` | `str` | `"processing_date_yymmdd"` | Parameters for saving metadata within the saved fields of view after illumination correction – name of processing date in metadata. This is a key in the metadata dictionary used to save metadata within the processed image. The key is associated with a value corresponding to the date the image was processed. When saved, it becomes an entry in the metadata saved within the processed image, indicating the date when the background correction was performed. | I would change the term illumination correction into background correction in the description |
| `processing_date_format` | `str` | `'%y%m%d'` | Parameters for saving metadata within the saved fields of view after illumination correction – date format in metadata. This is the format used for indicating the date when the background correction was performed in the metadata saved within the processed image. | I would change the term illumination correction into background correction in the description |
| `proc_img_meta_dtype_name` | `str` | `'dtype'` | Parameters for saving metadata within the saved fields of view after illumination correction – data type in metadata. This is a key in the metadata dictionary used to save metadata within the processed image. The key is associated with a value corresponding to the data type of the illumination-corrected image. When saved, it becomes an entry in the metadata saved within the processed image, indicating the data type of the background-corrected image. | this parameter can be ignored and dropped from the notebook |
| `illum_corr_method_metadata_entry` | `str` | `"illumination_correction_method"` | Parameters for saving metadata within the saved fields of view after illumination correction – illumination correction method in metadata. This is a key in the metadata dictionary used to save metadata within the processed image. The key is associated with a value corresponding to the `method` value used for background correction. When saved, it becomes an entry in the metadata saved within the processed image, indicating the `method` value used for background correction. | this parameter can be ignored and dropped from the notebook |
| `illum_corr_offset_metadata_entry` | `str` | `"illumination_correction_offset"` | Parameters for saving metadata within the saved fields of view after illumination correction – illumination correction offset in metadata. This is a key in the metadata dictionary used to save metadata within the processed image. The key is associated with a value corresponding to the `offset` value used for background correction. When saved, it becomes an entry in the metadata saved within the processed image, indicating the `offset` value used for background correction (if an offset was used). | this parameter can be ignored and dropped from the notebook |
| `illum_corr_rescale_metadata_entry` | `str` | `"illumination_correction_rescale_background"` | Parameters for saving metadata within the saved fields of view after illumination correction – illumination correction rescaling in metadata. This is a key in the metadata dictionary used to save metadata within the processed image. The key is associated with a value corresponding to the `rescale_background` value used for background correction. When saved, it becomes an entry in the metadata saved within the processed image, indicating the `rescale_background` value used for background correction. | this parameter can be ignored and dropped from the notebook |
| `illum_corr_clipping_metadata_entry` | `str` | `"illumination_correction_clipping"` | Parameters for saving metadata within the saved fields of view after illumination correction – illumination correction clipping in metadata. This is a key in the metadata dictionary used to save metadata within the processed image. The key is associated with a value corresponding to the `clip_corrected_image` value used for background correction. When saved, it becomes an entry in the metadata saved within the processed image, indicating the `clip_corrected_image` value used for background correction. | this parameter can be ignored and dropped from the notebook |
| `illum_corr_clip_min_value_metadata_entry` | `str` | `"illumination_correction_min_clip_value"` | Parameters for saving metadata within the saved fields of view after illumination correction – illumination correction clipping minimum value in metadata. This is a key in the metadata dictionary used to save metadata within the processed image. The key is associated with a value corresponding to the `min_clip_value` value used for background correction. When saved, it becomes an entry in the metadata saved within the processed image, indicating the `min_clip_value` value used for background correction, if clipping is performed. | this parameter can be ignored and dropped from the notebook |
| `illum_corr_clip_max_value_metadata_entry` | `str` | `"illumination_correction_max_clip_value"` | Parameters for saving metadata within the saved fields of view after illumination correction – illumination correction clipping maximum value in metadata. This is a key in the metadata dictionary used to save metadata within the processed image. The key is associated with a value corresponding to the `max_clip_value` value used for background correction. When saved, it becomes an entry in the metadata saved within the processed image, indicating the `max_clip_value` value used for background correction, if clipping is performed. | this parameter can be ignored and dropped from the notebook |
| `illum_corr_offset_background_metadata_entry` | `str` | `"illumination_correction_offset_background"` | Parameters for saving metadata within the saved fields of view after illumination correction – illumination correction `offset_background` in metadata. This is a key in the metadata dictionary used to save metadata within the processed image. The key is associated with a value corresponding to the `offset_background` value used for background correction. When saved, it becomes an entry in the metadata saved within the processed image, indicating the `offset_background` value used for background correction, if clipping is performed. | this parameter can be ignored and dropped from the notebook |
| `photometric` | `str` | `'minisblack'` | Indicate the photometric interpretation to be used when saving the files. | **NOTE:** At the moment, only `'minisblack'` has been tested. | this parameter is used because ome.tif files are saved. As I would switch to ome.zarr files, it can be ignored |
| `save_imagej_compatible` | `bool` | `True` | Indicate whether to save the background function image with ImageJ-compatible settings. | this parameter can be ignored and dropped from the notebook. It wans't even used |
| `column_name_separator` | `str` | `"_"` | Parameters for metadata dataframe updating – column name word separator. This is the separator used for separating the different parts of the column names to be added to the metadata dataframe. | this is a general parameter which is common across notebooks |
| `ch_name_separator` | `str` | `"-"` | Parameters for metadata dataframe updating – channel name separator. This is the separator used for separating the channel name from the rest of the column name to be added to the metadata dataframe. | this is a general parameter which is common across notebooks |
| `illum_correct_df_date_clm_name` | `str` | `f"illumination{column_name_separator}correction{column_name_separator}date"` | Parameters for metadata dataframe updating – illumination correction metadata dataframe date column name. This is the name of the column to be added to the metadata dataframe to indicate the day when the illumination correction was computed. | I would change the term illumination correction into background correction |
| `illum_correct_df_meta_date_format` | `str` | `'%y%m%d'` | Parameters for metadata dataframe updating – illumination correction metadata dataframe date format. This is the format used for indicating the date when the background correction was computed. This is used for saving the date in the metadata dataframe. | I would change the term illumination correction into background correction |
| `illum_correct_df_file_name_clm_name` | `str` | `f"illumination{column_name_separator}correction{column_name_separator}file{column_name_separator}name"` | Parameters for metadata dataframe updating – corrected file column name. This is the name of the column to be added to the metadata dataframe to indicate the name used for saving the background-corrected file. | I would change the term illumination correction into background correction |
| `illum_correct_df_method_clm_name` | `str` | `f"illumination{column_name_separator}correction{column_name_separator}method"` | Parameters for metadata dataframe updating – method for illumination correction column name. This is the name of the column to be added to the metadata dataframe to indicate the `method` used for background correction. | I would change the term illumination correction into background correction |
| `illum_correct_df_offset_clm_name` | `str` | `f"illumination{column_name_separator}correction{column_name_separator}offset"` | Parameters for metadata dataframe updating – offset for illumination correction column name. This is the name of the column to be added to the metadata dataframe to indicate the `offset` used for background correction, if an offset was used. | I would change the term illumination correction into background correction |
| `illum_correct_df_rescale_clm_name` | `str` | `f"illumination{column_name_separator}correction{column_name_separator}rescale{column_name_separator}background"` | Parameters for metadata dataframe updating – rescale for illumination correction column name. This is the name of the column to be added to the metadata dataframe to indicate whether the background function was rescaled when performing background correction and the method used. The `rescale_background` value is stored in the column named after this parameter. | I would change the term illumination correction into background correction |
| `illum_correct_df_clipping_clm_name` | `str` | `f"illumination{column_name_separator}correction{column_name_separator}clip{column_name_separator}output"` | Parameters for metadata dataframe updating – clipping for illumination correction column name. This is the name of the column to be added to the metadata dataframe to indicate whether the corrected image has been clipped when performing background correction. The `clip_corrected_image` value is stored in the column named after this parameter. | |
| `illum_correct_df_clip_min_value_clm_name` | `str` | `f"illumination{column_name_separator}correction{column_name_separator}min{column_name_separator}clip{column_name_separator}value"` | Parameters for metadata dataframe updating – clipping minimum value for illumination correction column name. This is the name of the column to be added to the metadata dataframe to indicate the minimum value used for clipping the corrected image, if clipping was performed. The `min_clip_value` value is stored in the column named after this parameter. | I would change the term illumination correction into background correction |
| `illum_correct_df_clip_max_value_clm_name` | `str` | `f"illumination{column_name_separator}correction{column_name_separator}max{column_name_separator}clip{column_name_separator}value"` | Parameters for metadata dataframe updating – clipping maximum value for illumination correction column name. This is the name of the column to be added to the metadata dataframe to indicate the maximum value used for clipping the corrected image, if clipping was performed. The `max_clip_value` value is stored in the column named after this parameter. | I would change the term illumination correction into background correction |
| `illum_correct_df_offset_background_clm_name` | `str` | `f"illumination{column_name_separator}correction{column_name_separator}offset{column_name_separator}background"` | Parameters for metadata dataframe updating – `offset_background` for illumination correction column name. This is the name of the column to be added to the metadata dataframe to indicate whether an offset has been used during background correction. The `offset_background` value is stored in the column named after this parameter. | I would change the term illumination correction into background correction |
| `save_file_name_separator` | `str` | `'_'` | Parameters for file saving – separator used for saved file names. | this is a general parameter which is common across notebooks |
| `project_name` | `str` | `"ACID"` | Parameters for file saving – project name. | this is a general parameter which is common across notebooks |
| `save_csv_index` | `bool` | `False` | Parameters for file saving – indicate whether to include indexes when saving pandas dataframes as CSV files. If `False`, the index will not be saved as a separate column in the CSV file. | this is a general parameter which is common across different csv files and notebooks |
| `fov_illumin_corrected_savingword` | `str` | `'bg'` | Parameters for file saving – illumination correction image name saving word. This is the word to use in the field of view file name to indicate that the file is an illumination-corrected field of view. | I would drop this parameter and save the corrected image with the same name of the input image |
| `ome_suffix` | `str` | `".ome.tif"` | Parameters for file saving – OME suffix used to save OME-TIFF files. | this is a general parameter which is common across notebooks |
| `metadata_savingword` | `str` | `"metadata"` | Parameters for file saving – processing metadata dataframe name saving word. | this is a general parameter which is common across notebooks |
| `metadata_file_suffix` | `str` | `f"part{save_file_name_separator}4b.csv"` | Parameters for file saving – processing metadata dataframe name file suffix. | I would drop this parameter and substitute it with: a first parameter called "notebook_savingword" and a second parameter called "csv_suffix". notebook_savingword does not have a space separtor nor a suffix (e.g. part4b). There would be a notebook_savingword per each notebook. csv_suffix would be a general use parameter whenever a csv file should be opened or save. It could be stored in a part of the config file independed from individual notebooks |
| `metadata_date_format` | `str` | `'%Y%m%d'` | Parameters for file saving – processing metadata dataframe name date format. | this is a general parameter which is common across notebooks |
| `hyperparameters_date_format` | `str` | `'%Y%m%d-%H%M%S'` | Parameters for file saving – hyperparameters dataframe name date format. | this is a general parameter which is common across notebooks |
| `hyperparameters_savingword` | `str` | `"hyperparameters"` | Parameters for file saving – hyperparameters dataframe name saving word. | this is a general parameter which is common across notebooks |
| `hyperparameters_file_suffix` | `str` | `f"part{save_file_name_separator}4b.csv"` | Parameters for file saving – hyperparameters dataframe name file suffix. | As I wrote for `metadata_file_suffix`, I would drop this parameter and substitute it with: a first parameter called "notebook_savingword" and a second parameter called "csv_suffix". Ref to `metadata_file_suffix` |
| `graph_date_format` | `str` | `'%Y%m%d'` | Parameters for displaying the results – graph saving date format. | this is a general parameter which is common across different graphs and notebooks |
| `graph_suffix` | `str` | `f".png"` | Parameters for displaying the results – graphs suffix. | I would rename this parameter as png_suffix. It is a general parameter common across graphs and notebooks. It can go in a dedicated section of a config file with saving parameters, including various types of suffixes (e.g. "csv_suffix", "ome_zarr_suffix" etc) |
| `graph_savingword` | `str` | `f"illumination{save_file_name_separator}correction"` | Parameters for displaying the results – graph saving word. | |
| `secondary_output_directory` | `str` | `"secondary_output"` | Parameters for saving secondary information – indicate the name of the directory for saving secondary outputs. This directory is used to store the hyperparameters used per each run of the pipeline. | |
| `exist_ok` | `bool` | `True` | Parameters for saving secondary information – if `True`, does not raise an error when a directory already exists. | |


